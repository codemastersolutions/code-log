watch:
    directories:
      - config
      - src
      - database
      - tests
      - stubs
    fileMask: '*.php*'
    notifications:
      passingTests: false
      failingTests: false
    phpunit:
      binaryPath: vendor/bin/phpunit
      arguments: '--stop-on-failure'
      timeout: 180
